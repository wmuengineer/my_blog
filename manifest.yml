---
applications:
- name: adam_blog
  routes:
    - route: adamdbell.com
    - route: www.adamdbell.com 
  services:
    - adam_blog_production
  command: bundle exec rake cf:on_first_instance db:migrate && bundle exec rails s -p $PORT -e $RAILS_ENV
  memory: 256M
  instances: 1
