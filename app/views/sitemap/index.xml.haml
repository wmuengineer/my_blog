!!! XML
%urlset{:xmlns => "http://www.sitemaps.org/schemas/sitemap/0.9"}

  %url
    %loc #{root_url}
    %lastmod= @default_last_mod.strftime('%Y-%m-%d')
    %changefreq weekly
    %priority 0.5

  - for post in @posts
    %url
      %loc #{root_url.chomp('/') + post.full_url}
      %lastmod= post.updated_at.strftime('%Y-%m-%d')
      %changefreq weekly
      %priority 0.5
